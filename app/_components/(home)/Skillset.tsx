"use client";
import React, { forwardRef } from "react";

const Skillset = forwardRef((props, ref: React.Ref<HTMLElement>) => {
  return <div>Skillset</div>;
});

Skillset.displayName = "Skillset";

export default Skillset;
